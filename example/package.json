{
    "name": "example",
    "version": "1.0.0",
    "description": "",
    "scripts": {
        "build": "rimraf build && tsc",
        "dbg": "npm run build && npx dbcodegen build build/schema.js src/generated"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "dependencies": {
        "@yesness/dbcodegen": "file:.."
    },
    "devDependencies": {
        "rimraf": "^3.0.2",
        "typescript": "^4.8.4"
    }
}
